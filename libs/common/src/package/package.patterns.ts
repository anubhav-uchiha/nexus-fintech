export const PACKAGE_PATTERNS = {
  CREATE: 'auth.package.create',
  FIND_ALL: 'auth.package.find-all',
  FIND_BY_ID: 'auth.package.find-by-id',
  UPDATE: 'auth.package.update',
  UPDATE_STATUS: 'auth.package.update-status',
} as const;
